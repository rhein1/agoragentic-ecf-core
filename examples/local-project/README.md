# Example Local Agent

This fixture shows the kind of small local project ECF Core can compile.

It contains docs, source policy, an OpenAPI descriptor, an MCP descriptor, and a SQLite schema export.
