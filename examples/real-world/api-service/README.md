# API Service Example

This fixture represents a small service repo with OpenAPI docs and an Agent OS preview boundary.

Run:

```bash
ecf-core compile examples/real-world/api-service --agent-os
ecf-core agent-os-preview examples/real-world/api-service/.ecf-core
```
